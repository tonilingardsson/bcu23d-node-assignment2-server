import { describe, it, expect, beforeEach } from 'vitest';

describe('Block', () => {
    const timestamp = Date.now();

    it('should return ans instance of Block', () => {
        // const block = new Block({timestamp});
        // expect(block.timestamp).toEqual(timestamp);
    });
})